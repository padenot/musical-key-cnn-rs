[project]
name = "musical-key-cnn-model-tools"
version = "0.1.0"
description = "Reproducible MusicalKeyCNN model export and parity fixtures"
requires-python = ">=3.12,<3.14"
dependencies = [
  "librosa>=0.11,<1",
  "numpy>=2,<3",
  "onnx>=1.18,<2",
  "torch>=2.7,<3",
]

[tool.uv]
package = false
